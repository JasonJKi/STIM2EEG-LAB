CPP=g++

#vpath %.c RecorderLib
VPATH ../RecorderLib
#vpath %.h RecorderLib

#CXXFLAGS = -fPIC
#INCLUDE = -I../ -I../../../../../../LSL/liblsl/include/ $(shell pkg-config --cflags python)
#LIBPATH = 
#LIBS = $(shell pkg-config --libs python)

#SRCS = ../RecorderLib.cpp 
#../RecorderLib_wrap.c
#OBJS = $(SRCS:.cpp=.o)
#OBJS += $(SRCS:.c=.o)

#.PHONY: clean rlib.o RecorderLib64.so 

#all: RecorderLib64.so rlib.o
all: rlib.o

rlib.o: RecorderLib.cpp #RecorderLib.h
	$(CPP) ../RecorderLib.cpp

#%.o: %.c %.cpp
#	$(CPP) $(CXXFALGS) $(LIBPATH) $(LIBS) $(INCLUDE)-c $@ -o  $^

#RecorderLib64.so: $(OBJS)
#RecorderLib64.so: rlib.o	
#	ld -shard -soname $@.1 -o $@.1.0 $(OBJS)
#	ln -s ./RecorderLib64.so.1.0 RecorderLib64.so.1
#	ln -s ./RecorderLib64.so.1 RecorderLib64.os

#clean:
	rm -f *.so* *.o
